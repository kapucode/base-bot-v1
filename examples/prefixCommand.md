```javascript;
  module.exports = {
    name: 'example',
    guildOnly: true, // opcional
    devOnly: false, // opcional
    
    run: async (client, message, args) => {
      // lógica 
    };
  };
```